version: "3.9"

services:
  voyageai:
    build: .
    container_name: voyageai_app
    ports:
      - "8000:8000"          # Access at http://localhost:8000
    env_file:
      - .env                  # All API keys from .env file
    volumes:
      - ./output:/app/output  # Travel guides saved locally
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
